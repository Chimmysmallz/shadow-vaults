{
  "name": "shadow-vaults-android",
  "version": "1.0.0",
  "description": "Android wrapper for the Shadow Vaults dungeon adventure (Capacitor).",
  "license": "MIT",
  "scripts": {
    "sync": "node -e \"require('fs').cpSync('../','www',{recursive:true,filter:p=>!p.includes('android')&&!p.includes('node_modules')&&!p.includes('.git')})\"",
    "open": "cap open android",
    "build": "cap sync android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}
